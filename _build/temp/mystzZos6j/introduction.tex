% Created with jtex v.1.0.20
\documentclass{article}
\usepackage{hyperref}
\usepackage{datetime}
\usepackage{graphicx}
\usepackage{natbib}
\bibliographystyle{abbrvnat}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
%%%%%%%%%%%%%%%%%%%%  imports  %%%%%%%%%%%%%%%%%%%
\usepackage{framed}
\usepackage{url}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


% colors for hyperlinks
\hypersetup{colorlinks=true, allcolors=blue}

\newcommand{\logo}{
  \href{https://curvenote.com}{\includegraphics[width=2cm]{curvenote.png}}
}

\title{How to Write and Publish a Book}

\author{Qiusheng Wu}

\newdate{articleDate}{14}{11}{2025}
\date{\displaydate{articleDate}}

\begin{document}
\maketitle
\begin{abstract}This tutorial introduces how to write and publish a book using Jupyter Book.\end{abstract}\begin{center}\logo\end{center}


Whether you write your book's content in Jupyter Notebooks (\texttt{.ipynb}) or
in regular markdown files (\texttt{.md}), you'll write in the same flavor of markdown
called \textbf{MyST Markdown}.

\section{What is MyST?}

MyST stands for ``Markedly Structured Text''. It
is a slight variation on a flavor of markdown called ``CommonMark'' markdown,
with small syntax extensions to allow you to write \textbf{roles} and \textbf{directives}
in the Sphinx ecosystem.

\section{What are roles and directives?}

Roles and directives are two of the most powerful tools in Jupyter Book. They
are kind of like functions, but written in a markup language. They both
serve a similar purpose, but \textbf{roles are written in one line}, whereas
\textbf{directives span many lines}. They both accept different kinds of inputs,
and what they do with those inputs depends on the specific role or directive
that is being called.

\subsection{Using a directive}

At its simplest, you can insert a directive into your book's content like so:

\begin{verbatim}
```{mydirectivename}
My directive content
```
\end{verbatim}

This will only work if a directive with name \texttt{mydirectivename} already exists
(which it doesn't). There are many pre-defined directives associated with
Jupyter Book. For example, to insert a note box into your content, you can
use the following directive:

\begin{verbatim}
```{note}
Here is a note
```
\end{verbatim}

This results in:

\begin{framed}
\textbf{Note}\\
Here is a note
\end{framed}

In your built book.

For more information on writing directives, see the
\href{https://myst-parser.readthedocs.io/}{MyST documentation}.

\subsection{Using a role}

Roles are very similar to directives, but they are less-complex and written
entirely on one line. You can insert a role into your book's content with
this pattern:

\begin{verbatim}
Some content {rolename}`and here is my role's content!`
\end{verbatim}

Again, roles will only work if \texttt{rolename} is a valid role's name. For example,
the \texttt{doc} role can be used to refer to another page in your book. You can
refer directly to another page by its relative path.

For more information on writing roles, see the
\href{https://myst-parser.readthedocs.io/}{MyST documentation}.

\subsection{Adding a citation}

You can also cite references that are stored in a \texttt{bibtex} file. For example,
the following syntax: \texttt{\{cite\}`holdgraf\_evidence\_2014`} will render like
this: \cite{holdgraf_evidence_2014}.

Multiple citations can be used like this:
\citet{holdgraf_rapid_2016, holdgraf_encoding_2017}

Moreover, you can insert a bibliography into your page with this syntax:
The \texttt{\{bibliography\}} directive must be used for all the \texttt{\{cite\}} roles to
render properly.
For example, if the references for your book are stored in \texttt{references.bib},
then the bibliography is inserted with:

\begin{verbatim}
```{bibliography}
```
\end{verbatim}

Resulting in a rendered bibliography that looks like:

%  ```{bibliography}
% 
% ```

\subsection{Executing code in your markdown files}

If you'd like to include computational content inside these markdown files,
you can use MyST Markdown to define cells that will be executed when your
book is built. Jupyter Book uses \textit{jupytext} to do this.

First, add Jupytext metadata to the file. For example, to add Jupytext metadata
to this markdown page, run this command:

\begin{verbatim}
jupyter -book myst init markdown.md
\end{verbatim}

Once a markdown file has Jupytext metadata in it, you can add the following
directive to run the code at build time:

\begin{verbatim}
```{code -cell}
print("Here is some code to execute")
```
\end{verbatim}

When your book is built, the contents of any \texttt{\{code-cell\}} blocks will be
executed with your default Jupyter kernel, and their outputs will be displayed
in-line with the rest of your content.

For more information about executing computational content with Jupyter Book,
see \href{https://myst-nb.readthedocs.io/}{The MyST-NB documentation}.


\bibliography{main.bib}
\end{document}
